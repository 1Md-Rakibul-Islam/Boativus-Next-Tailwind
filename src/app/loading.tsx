const Loading = () => {
  return (
    <main className="bg-black">
      <section className="min-h-screen w-full bg-black bg-no-repeat bg-center">
        <div className="preloader">
          <span className="loader"></span>
        </div>
      </section>
    </main>
  );
};

export default Loading;
